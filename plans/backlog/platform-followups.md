# Platform Follow-ups

## Remaining platform work

1. Fix the asset pipeline so CSP no longer blocks it.
2. Configure OpenAPI now that Rswag has been added.
3. Reconfigure the Rails → Cloud Run → Cloud Load Balancer → Fastly path.
4. Configure AWS SES with the `.com`, `.app`, and `.org` domains for production once those domains
   are added.
